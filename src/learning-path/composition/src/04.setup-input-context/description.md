# setup 上下文示例

这个示例展示了 Vue 3 中 setup 函数如何接收和使用上下文对象。

## 功能说明
- setup 函数接收 context 参数
- context 对象包含的属性
- context 在 setup 中的使用

## 学习要点
- setup 函数的第二个参数
- context 对象的属性
- 组件实例的访问方式
- 上下文属性的使用

## 关键特性
- context 包含 attrs、slots、emit 等属性
- 可以通过 context 访问组件实例
- 上下文属性的响应式特性
- 组件通信的方式 