# setup 接收 props 示例

这个示例展示了 Vue 3 中 setup 函数如何接收和使用 props。

## 功能说明
- setup 函数接收 props 参数
- props 的类型定义和默认值
- props 在 setup 中的使用

## 学习要点
- setup 函数的参数
- props 的定义方式
- props 的类型检查
- props 的默认值设置

## 关键特性
- setup 函数可以接收 props 作为第一个参数
- props 是响应式的，可以安全地解构
- props 的类型定义和验证
- props 的默认值处理 