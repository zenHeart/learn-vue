# Vue 3 模态框传送示例

该示例展示了如何使用 Vue 3 的 `teleport` 功能实现模态框。

## 功能说明
- 使用 `teleport` 将模态框传送到指定的 DOM 节点。
- 实现模态框的显示和隐藏。
- 处理模态框的事件通信。

## 学习要点
- `teleport` 的基本用法。
- 模态框的状态管理。
- 模态框的事件处理和样式设计。

## 关键特性
- `teleport`
- 状态管理
- 事件通信