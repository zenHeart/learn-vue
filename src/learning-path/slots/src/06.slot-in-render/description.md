# 渲染中的插槽示例

这个示例展示了在 Vue 3 的渲染函数中使用插槽的基本方法。

## 功能说明
- 在渲染函数中访问插槽内容
- 使用 $slots 访问默认插槽
- 插槽内容的渲染方式
- 插槽的统一访问机制

## 学习要点
- 渲染函数中插槽的使用
- $slots 对象的访问
- 默认插槽的渲染
- 插槽内容的组合

## 关键特性
- 渲染函数
- 插槽访问
- 内容渲染
- 统一机制 